% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random_puzzle.R
\name{random_puzzle}
\alias{random_puzzle}
\title{Generate a random solvable Slitherlink puzzle}
\usage{
random_puzzle(n = 5L, m = 5L, seed = NULL, max_tries = 50L)
}
\arguments{
\item{n}{Integer. Number of rows (must be >= 2, default 5).}

\item{m}{Integer. Number of columns (must be >= 2, default 5).}

\item{seed}{Optional integer passed to \code{set.seed()} for
reproducibility.}

\item{max_tries}{Maximum generation attempts before giving up
(default 50).}
}
\value{
A \code{slitherlink_grid} with clues set and no segments drawn.
}
\description{
Creates a puzzle by first generating a random valid closed loop on an
n x m grid (via random region growth), then deriving the clue counts
from that loop.  Puzzles are verified to have a unique solution before
being returned; attempts that yield multiple solutions are discarded.
}
