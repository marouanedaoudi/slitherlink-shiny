# Check if a Slitherlink grid is fully solved

A grid is solved when all clue constraints are exactly satisfied and the
drawn segments form a single closed loop.

## Usage

``` r
is_solved(grid)
```

## Arguments

- grid:

  An object of class 'slitherlink_grid'.

## Value

A logical value: TRUE if the puzzle is solved.
